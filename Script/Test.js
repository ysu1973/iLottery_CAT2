function dblclickTest()
{
  //Simulates a left-button double click in a window or control as specified (relative position, shift keys).
  Aliases.browser.pageRegistration.formConfirmregistration.passwordboxUserPassword.DblClick();
}